# Explicit, assumed rule set — CI must not drift when a new ruff release widens
# its defaults (0.16 added rules that flag deliberate patterns here, e.g. the
# fail-open blind excepts in hooks). Widen this list on purpose, not by upgrade.
lint.select = ["E4", "E7", "E9", "F"]
